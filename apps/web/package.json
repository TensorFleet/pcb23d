{
  "name": "@pcb23d/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "preview": "astro preview",
    "typecheck": "astro check",
    "cf:dev": "bun run build && wrangler dev --config wrangler.jsonc",
    "cf:dry-run": "bun run build && wrangler deploy --dry-run --config wrangler.staging.jsonc && wrangler deploy --dry-run --config wrangler.production.jsonc",
    "deploy:staging": "bun run build && wrangler deploy --config wrangler.staging.jsonc",
    "deploy:production": "bun run build && wrangler deploy --config wrangler.production.jsonc"
  },
  "dependencies": {
    "@astrojs/check": "0.9.4",
    "astro": "5.13.3",
    "fflate": "0.8.2",
    "jpeg-js": "0.4.4",
    "pcb23d": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "1.2.21",
    "typescript": "5.9.2",
    "wrangler": "^4.127.1"
  }
}
